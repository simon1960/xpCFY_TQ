Further actions to be added to the calibration and live display windows:-

1. The speed brake lever needs to be pulled down when ever it is in the closed or stop position. Add a function to accomplish this. 
2. In the calibration window, add a button to close the window when the calibration has been saved. 
3. On the live position window, add four buttons which perform the following functions:-
	a) Speed brake retract and pull down.
	b) Speed brake push up and move to full extension.
	c) Parking brake interlock release.
	d) Parking brake interlock set.

	For A & B, ensure that the flight detent interlock is retracted.


Add the following test button to the live display window.

1. Test Throttles:
	a) Drive the left throttle handle forward to fully open position at slow speed then return it to fully closed position.

	followed by ...

	b) Drive the left throttle handle forward to fully open position at medium speed then return it to fully closed position.

	followed by ...

	c) Drive the right throttle handle forward to fully open position at slow speed then return it to fully closed position.

	followed by ...

	d) Drive the right throttle handle forward to fully open position at medium speed then return it to fully closed position.

	followed by ...

	e) drive both throttle handles forward to fully open position at medium speed then return them to fully closed position.

Fully document PoKeys pin mappings and and functions then write to file ready for upload to GitHub.


Update the throttle test as follows: when testing both throttles together, move at fast speed.

Add a close window button to the live display window.

Add the copyright message to all windows.

Deploy plugin to test PC.


Update the speedbrake buttons as follows:-

	a) When speed brake is retracted, set the background colour of the retract button GREEN and the background of the extend button AMBER.
	b) When speed brake is extended, set the background colour of the retract button AMBER and the background of the extend button GREEN.

Update the parking brake buttons following the logic for the speedbrake buttons.

The copyright notice is not appearing on the windows. Fix this.

Deploy plugin to test PC.

========================

The dataref registration and data gatherer functions have been created and may be found in acf_dref.c. The file contains a list of datarefs with comments against each entry
detailing the enumerate to use as an index into the table and the corresponding FSUIPC offset. 

datastructures.h contains the dref table structure and aircraft data block.

Implement the TQ control logic contained in the original .NET application, preserving the existing code for the plugin. When adding code, document the code in the comments so that
the plugin can be easily maintained.

The control and test buttons in the live display window can only be displayed when the battery master switch is off and the aircraft is on the ground. refer to the aircraft data block
for the correct members to check.

Do not make asssumptions when adding code. If further information is required, pause the design and request advice or information from me.

If additional datarefs are required to supply data that is not present in the aircraft data block, pause the design and request assistance from me.




1. Throttle override state should not be required for X-Plane as the throttle values read from the PoKeys ADC should be mapped to a value between 0.0f and 1.0f and written directly
   to the datarefs in the DREF table referenced by DREF_THROTTLE_RATIO_LT (for the left throttle) and DREF_THROTTLE_RATIO_RT (for the right throttle).

2. When manally operated, the speed brake position should be read from the PokEys ADC and the value mapped between 0.0f to 1.0f and written directly to the
   dataref in the DREF table referenced by DREF_SPD_BRAKE_LEVER.

All data writes to X-Plane must occur when a change is detected and follow directly after GetDatarefValues() in the data gatherer FLCB function.

Explain the signficance of the Engine Fire indicators with regard to the original logic.

Ignore the Flaps for now. Further investigation is required by me.

The discrepancy over at_active has been resolved.

1. While at_active, inhibit throttle writes.

2. Retain 10 sample moving average as per the original application.

3. Retain original Spedbrake writes irrespective of movement origin.

4. What do PoKeys digital outputs 36 & 37 drive? What is the logic flow for the fire indicators?

5. Build for V3, V4 and Pro variants.



An additional dataref has been added to acf_dref.c to collect the parking brake lamp state from the simulator. Use acData.pbIndicator to set the Indicator Lamp. OPTION_ENERGIZE_STATE_OF_AIRCRAFT_INFLUENCES_PB_LIGHT should be enabled so that when the battery master is off, the lamp remains off.
To set the parking brake, use the existing logic and write 1.0f directly to the dataref in the DREF table referenced by DREF_PARKING_BRAKE.

Update the interlock state directly from the plugin. If a new dataref is required, pause and let me know.

The parking brake blink is not required, remove any code connected with the blink from the plugin.

Deploy the code to the test PC.

========================

1. Add the TO/GA button code.
	a. Intercept the left TOGA button state in the PoKeys loop and write the value as an (int) to state->lt_toga. Call left_toga_handler (declared in adc_dref.h) passing the state table as (void*) state.
	b. Intercept the right TOGA button state in the PoKeys loop and write the value as an (int) to state->rt_toga. Call rightt_toga_handler (declared in adc_dref.h) passing the state table as (void*) state.

Build the solution and deploy to the Test PC. 

Add the A/T disconnect button code.
	a. Intercept the left A/T disconnect button state (PoKeys pin 4) in the PoKeys loop and write the value as an (int) to state->lt_at_disco. Call left_at_disco_handler (declared in adc_dref.h) passing the state table as (void*) state.
	b. Intercept the right A/T disconnect button state (PoKeys pin 5) in the PoKeys loop and write the value as an (int) to state->rt_at_disco. Call rightt_at_disco_handler (declared in adc_dref.h) passing the state table as (void*) state.

2. The parking brake is still not working. Evaluate the logic in the original .NET app and compare to the current logic in the plugin. Correct as necessary.

3. Add the reverser handler logic into the plugin.

4. Update the log handler so that when the plugin starts any existing log file is deleted and a new log file is started.

Build the solution and deply to the test PC


The parking brake is still not working. On plugin startup, the parking brake interlock is being set when it should be released and the parking brake indicator is permanently on even when the battery bus is off. Using the parking brake release button in the live display window allows me to clear the interlock and when applying power allows me to set the parking brake. When the toe brakes are pressed the parking brake does not release. Investigate the issue and correct the parking brake code.


========================

Initial testing has completed and the following defects have been found.

1. Trim Main Elec switch is inverted.
2. Trim Auto Pilot switch is inverted.
3. Parking Brake lamp is inverted.
4. Trim Wheel motor is not operating when trim changes.

Correct all defects, update documentation and deploy to test PC.

1. Parking brake is now working correctly. remove all PB debug code from solution.
2. The trim wheel motor is taking too long to engage. Following a trim change either by trim switch or autopilot trim control, the motor can take up to 5 seconds to respond resulting in an incorrect trim setting being sent back to X-Plane by the PoKeys ADC read and FLCB functions. The motor engage should be immediate. Investigate, correct and deploy to test PC.


Further TQ testing has taken place and the following defects have been found.

1. When A/T is engaged and before selecting TO/GA, moving the throttles manually does not change the thrust ratio.
2. When A/T is engaged and after selecting TO/GA, the FMC increases thrust but the throttle levers are not advanced by the motors.
3. Manual trim control by trim switch action is unreliable/intermittent and does not work at all when A/P is engaged.
4. Parking brake releases on toe control but does not flag to X-Plane that parking brake has been released.
5. Flaps control and all switches are working well.
6. Speedbrake has not been tested yet.

Investigate, correct and deploy to test PC.


Two more defects have been identified.

1. When under A/T or A/P control, the throttles are perpetually hunting. Check the deadband and correct.
2. When A/T is engaged and A/P is disengaged, I have no manual control of trim. The plugin should only deny manual changes when A/P is engaged.

Correct, build and deploy to test PC.

The manual trim control still does not work.
The speedbrake extends when it is in the Arm position. It does not autoextend on landing and does not autoretract when throttles ar pushed forward after landing.

Correct and deploy to test PC.


1. Trim motor is still not working. Manual trim switch on yoke changes trim on sim but not on TQ. TQ trim indicator does not change. The yoke switch is mapped to the following X-Plane commands, sim/flight_controls/pitch_trim_down_elec and sim/flight_controls/pitch_trim_up_elec.
2. 






















